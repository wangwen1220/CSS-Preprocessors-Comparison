/* color functions */
red(#c00) // => 204
red(#000, 255) // => #f00
alpha(#fff) // => 1
alpha(rgba(0,0,0,0.3)) // => 0.3
dark(#005716) // => true
dark(white) // => false
light(white) // => true
light(#00FF40) // => true
hue(hsla(50deg, 100%, 80%)) // => 50deg
hue(#00c, 90deg) // => #6c0


/* hsl function */
blend(rgba(#FFF, 0.5), #000) // => #808080
lighten(#2c2c2c, 30%) // => #393939
darken(#D62828, 30) // => #551010
nvert(#d62828) // => #29d7d7
grayscale(#fd0cc7) // => #0cfd42
tint(#fd0cc7,66%) // => #feaceb
shade(#fd0cc7,66%) // => #560443
transparentify(#808080) // => rgba(0,0,0,0.5) ❤️
contrast(#000, #fff).ratio // => 21 ❤️


/* opacity funcitons */


/* string functions */


/* math functions */


/* list functions */
nums = 1 2
push(nums, 3, 4, 5)
nums // => 1 2 3 4 5

nums = 4 5
unshift(nums, 3, 2, 1)
nums // => 1 2 3 4 5

list2 = a, b, c
list-separator(list2) // => ','

join(',', 1 2 3) // => "1,2,3"

/* map functions */
pairs = (one 1) (two 2) (three 3)
keys(pairs) // => one two three

pairs = (one 1) (two 2) (three 3)
values(pairs) // => 1 2 3


typeof(#fff) // => 'rgba'
type(12) // => 'unit'

unit(10) // => ''
unit(15in) // => 'in'
unit(15%, px) // => 15px


/* introspection functions */


/* plugin */
use("plugins/add.js")
width add(10, 100) // => width: 110

// var plugin = function(){
//   return function(style){
//     style.define('add', function(a, b) {
//       return a.operate('+', b);
//     });
//   };
// };
// module.exports = plugin;
